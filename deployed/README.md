# Deployed Compatibility Baselines

This directory stores public, version-controlled evidence used to validate a
future Motoko toolchain upgrade.

`capture-backend-baseline.sh` writes the deployed backend's public Candid
metadata and a small JSON record of the source revision that captured it.

These files contain no user memories, provider credentials, private Aion
intelligence source, or canister state exports.
